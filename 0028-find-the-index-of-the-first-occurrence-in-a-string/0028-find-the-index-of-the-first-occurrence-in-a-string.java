class Solution {
    public int strStr(String haystack, String needle) {
        // for(int i = 0; i < haystack.lengtht(); i++) {
            // if(haystack.indexOf(needle))
            return haystack.indexOf(needle);
                // return ;
        // }
        // return -1;
    }
}